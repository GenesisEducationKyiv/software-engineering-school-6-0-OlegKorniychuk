GITHUB_TOKEN=token
# Optional: write logs to file (Filebeat reads this path in docker-compose)
# LOG_FILE=/var/log/github-release-notifier/app.log
EMAIL_SERVICE=Gmail # visit https://nodemailer.com/smtp/well-known-services for a list of supported services
EMAIL_SERVICE_USERNAME=username@gmail.com
EMAIL_SERVICE_PASSWORD=password
