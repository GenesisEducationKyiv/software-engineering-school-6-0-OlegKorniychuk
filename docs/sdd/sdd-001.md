# SDD-001: Github Repository Scanner

## 1. System Requirements

### Functional Requirements

- User should be able to subscribe to release updates for a github repository
- In order to subscribe for updates user should provide his email address and confirm it by clicking a link in an email
- After subscribing user should start receiving updates via email
- User should be able to unsubscribe from notifications by clicking a link provided with every notification email
- User should be able to view a list of his subsctiptions by email
- Service should expose a REST API

### Non-functional Requirements

- **High Reliability** - SLA 99%
- **Responsivness** - <400ms response time
- **Scalability** - should be ready to send 2000 notifications in 1 update cycle without increasing response time
- **Ease of Use** - confirming email & unsubscribing from notifications should be done in 1 click

### Constraints

- **Time** - 1 week
- **Team** - 1 engineer
- **Technologies** - lean web framework (Express or Fastify)

## 2. System Overview

### Tech Stack

- Framework - ExpressJS
- Database - Postgres
- ORM - DrizzleORM
- Containerization - Docker & docker-compose
- Migrations - Drizzle Kit
- Email Agent - nodemailer
- Caching - Redis
- Task Scheduling - BullMQ
- Task Queue - BullMQ
- Monitoring - Prometheus

### API Endpoints

- POST /subscribe { email: string; repo: string; }
- GET /confirm:/token
- GET /unsubscribe/:token
- GET /subscriptions?email

### Database Schema

```mermaid
erDiagram
  github_repositories ||--o{ subscriptions : "has many"

  github_repositories {
    uuid id PK "defaultRandom()"
    varchar name UK "not null"
    varchar last_seen_tag
  }

  subscriptions {
    uuid id PK "defaultRandom()"
    varchar email "not null"
    uuid github_repository_id FK "not null"
    boolean confirmed "not null, default(false)"
  }
```

### High-level Architecture

```mermaid
graph TD
    S[Scheduler]
    C[Cache]
    RS[Repository Scanner]
    EQ[Email Queue]
    SS[Subscription Service]
    EN[Email Notifier]

    S --> RS
    S --> EQ
    C --> SS
    SS --> RS
    SS --> EQ
    EQ --> EN
```

## 3. Application Flows

- Subscription Flow

```mermaid
  sequenceDiagram
    autonumber
    actor Client
    participant SS as Subscription Service
    participant RS as Repository Scanner
    participant EN as Email Notifier
    participant DB@{ "type": "database" }

    Note over Client, SS: Initiating Subscription
    Client->>SS: POST /subscribe {email, repo_name}

    Note over SS, RS: Verification Phase
    SS->>RS: Request check: does repo_name exist?

    alt Repo does NOT exist (False)
      RS-->>SS: False (Not Found)
      SS-->>Client: 404 Not Found
    else Repo exists (True)
      RS-->>SS: True (Found)

      Note over SS, Client: Completion Phase
      SS->>DB: Save {email, repo_name}
      DB-->>SS: Success
      SS->>EN: Trigger: send confirmation email
      EN->>Client: Send: "Subscription Confirmation"
    end
```

- Confirm Subscription Flow

```mermaid
sequenceDiagram
  autonumber
  actor Client
  participant SS as Subscription Service
  participant TS as Token Service
  participant DB@{ "type": "database" }

  Note over Client, SS: Confirmation Phase
  Client->>SS: GET /confirm/:token

  Note over SS, TS: Token Validation
  SS->>TS: Request: Is token correct?

  alt Token Invalid (No)
    TS-->>SS: False (Invalid Token)
    SS-->>Client: 400 Bad Request
  else Token Valid (Yes)
    TS-->>SS: True {email, repo_name}

    Note over SS, DB: Database Update
    SS->>DB: Confirm subscription (repo_name, email)
    DB-->>SS: Success

    SS-->>Client: 200 OK
  end
```

- View Subscriptions Flow

```mermaid
sequenceDiagram
  autonumber
  actor Client
  participant SS as Subscription Service
  participant DB@{ "type": "database" }

  Note over Client, SS: View Subscriptions
  Client->>SS: GET /subscriptions?email={email}

  Note over SS, DB: Data Retrieval
  SS->>DB: Query: Find subscriptions by email
  DB-->>SS: Return: subscription[] (Array)

  SS-->>Client: 200 OK {subscription[]}
```

- Unsubscribe Flow

```mermaid
sequenceDiagram
  autonumber
  actor Client
  participant SS as Subscription Service
  participant DB@{ "type": "database" }

  Note over Client, SS: View Subscriptions
  Client->>SS: GET /subscriptions?email={email}

  Note over SS, DB: Data Retrieval
  SS->>DB: Query: Find subscriptions by email
  DB-->>SS: Return: subscription[] (Array)

  SS-->>Client: 200 OK {subscription[]}
```
